name: Build FridaysMenu

on:
  push:
    paths:
      - 'FridaysMenu.m'

jobs:
  build:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v3

      - name: Install Theos
        run: |
          bash -c "$(curl -fsSL https://raw.githubusercontent.com/theos/theos/master/bin/install-theos)"

      - name: Download SDK
        run: |
          curl -LO https://github.com/theos/sdks/releases/download/current/iPhoneOS16.5.sdk.tar.xz
          tar -xf iPhoneOS16.5.sdk.tar.xz -C $THEOS/sdks

      - name: Compile dylib
        run: |
          clang -shared -o FridaysMenu.dylib FridaysMenu.m \
          -framework UIKit \
          -framework Foundation \
          -framework QuartzCore \
          -fobjc-arc \
          -target arm64-apple-ios14.0 \
          -isysroot $THEOS/sdks/iPhoneOS16.5.sdk

      - name: Upload dylib
        uses: actions/upload-artifact@v3
        with:
          name: FridaysMenu
          path: FridaysMenu.dylib
